# Bucket Studio vX.Y.Z

Bucket Studio is a free, local-first desktop browser for S3-compatible storage.

## Downloads

| Platform | Artifact |
| --- | --- |
| Windows x64 | `bucket-studio-vX.Y.Z-windows-x64.zip` |
| macOS | `bucket-studio-vX.Y.Z-macos.zip` |
| Linux x64 | `bucket-studio-vX.Y.Z-linux-x64.tar.gz` |

Verify downloads with `SHA256SUMS.txt`.

## Highlights

- Initial Windows beta release.

## Fixes

- First public beta build.

## Known Issues

- Source code is private during beta while the app depends on a private company UI package.
- macOS builds may require right-click Open until Developer ID signing and notarization are complete.
- Linux packaging starts as a tar.gz bundle; AppImage/Flatpak packaging is planned after beta feedback.
